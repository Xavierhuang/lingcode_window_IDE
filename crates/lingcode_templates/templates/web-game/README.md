# Web / HTML5 game

A zero-dependency canvas game scaffolded by LingCode. No build step.

## Run

Open `index.html` in any browser (double-click it), or serve it locally:

    python -m http.server   # then visit http://localhost:8000

Edit `game.js` to change the game loop.
